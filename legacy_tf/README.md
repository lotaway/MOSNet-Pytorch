# Legacy TensorFlow Code

This directory stores historical TensorFlow/Keras implementation files that are no longer the default runtime path.

- `model.py`: original Keras model definitions (CNN / BLSTM / CNN-BLSTM).
- `requirements.txt`: legacy TF environment dependencies.

Current default train/test/inference entrypoints in the project root use PyTorch.
